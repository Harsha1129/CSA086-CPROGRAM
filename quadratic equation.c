#include <stdio.h>
#include <conio.h>
 void main()
{
 int a,b,c,root1,root2;
 int d;
 Printf("Enter the value a \n");
 Scanf("%d",&a);
 Printf("Enter the value b \n");
 Scanf("%d",&b);
 Printf("Enter the value c \n");
 Scanf("%d",&c);
 d=((b*b)-(4*a*c));
if(d>0)
 {
 root1=(-b+Sqrt((b*b)-(4*a*c))/(2*a);
 root2=(-b-Sqrt((b*b)-(4*a*c))/(2*a);
 Printf("\n first root is %d",root1);
 Printf("\n second root is %d",root2);
 Printf("\n the root are real&unequal");
 }
elseif(d==0)
 {
  root1=(-b+Sqrt((b*b)-(4*a*c))/(2*a);
  root2=(-b-Sqrt((b*b)-(4*a*c))/(2*a);
 Printf("\n first root is %d",root
